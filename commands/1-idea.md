---
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - AskUserQuestion
---

# Capture Idea

If the user provided an idea alongside this command, pass it to the `capture-ideas` skill instructions below.

If no idea was provided, ask: "What idea would you like to capture?"

Then follow the `capture-ideas` skill to record it in the PlanKit ideas file.

$ARGUMENTS
